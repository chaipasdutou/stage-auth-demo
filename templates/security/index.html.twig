{% extends 'base.html.twig' %}

{% block title %}Espace sécurisé{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto bg-white p-8 rounded-xl shadow-lg mt-16">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold">Espace sécurisé</h1>
        <a href="{{ path('app_logout') }}" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition duration-200">
            Se déconnecter
        </a>
    </div>

    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
        <h2 class="font-bold">Bienvenue {{ app.user.email }} ! 👋</h2>
        <p>Vous êtes maintenant connecté(e) à votre espace sécurisé.</p>
    </div>

    <div class="bg-gray-50 p-6 rounded-lg">
        <h3 class="text-lg font-bold mb-4">Informations de votre compte</h3>
        <ul class="space-y-2">
            <li><strong>Email :</strong> <code class="bg-gray-200 px-2 py-1 rounded">{{ app.user.email }}</code></li>
            <li><strong>Rôles :</strong> 
                {% if app.user.roles %}
                    {% for role in app.user.roles %}
                        <span class="inline-block bg-blue-100 text-blue-800 px-2 py-1 rounded text-sm mr-1">{{ role }}</span>
                    {% endfor %}
                {% else %}
                    <span class="text-gray-500">Aucun rôle</span>
                {% endif %}
            </li>
        </ul>
    </div>
</div>
{% endblock %}
